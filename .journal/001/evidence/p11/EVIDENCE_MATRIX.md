# P11 Evidence Matrix

**Decision context:** P11 teardown passed. This matrix evaluates coverage, not production adoption; [`GO_NO_GO.md`](./GO_NO_GO.md) makes that decision.

| # | Coverage area | Result | Evidence and limit |
|---:|---|---|---|
| 1 | Baseline and read-only TPM inventory | **PASS** | [`../p0/EVIDENCE.md`](../p0/EVIDENCE.md) records the IncusOS safety baseline, persistent handle `0x81000001`, NV indices `0x01C00002` and `0x01C0000A`, and zero lockout counter. |
| 2 | Spike PKI and TPM-resident LDevID provisioning | **PASS** | [`../p1/EVIDENCE.md`](../p1/EVIDENCE.md) proves the chain, SPIRE-compatible DevID blobs, post-reboot loading, and zero persistent TPM footprint. The spike CA and blobs were deleted in P11 without a cryptographic-erasure claim. |
| 3 | SPIRE Server placement and `tpm_devid` configuration | **PASS for the spike** | [`../p2/EVIDENCE.md`](../p2/EVIDENCE.md) proves server health, restart/reboot persistence, and both attestor CA bundles. Same-host server placement was explicitly spike-only. |
| 4 | Host agent OCI packaging and physical TPM access | **PASS** | [`../p3/EVIDENCE.md`](../p3/EVIDENCE.md) proves the stock v1.15.2 agent used `/dev/tpmrm0` and produced the expected node identity. |
| 5 | Positive and negative `tpm_devid`; restart/reboot persistence | **PASS with recorded coverage limit** | P3 Gate G3 passed all four negative cases and preserved the TPM namespace. P10 case 1 separately forced fresh post-reboot `tpm_devid`; the foreign-key case failed earlier at local TPM load, so the server-side mismatched-EK branch was not exercised. |
| 6 | Standard Workload API before Broker integration | **PASS** | [`../p4/EVIDENCE.md`](../p4/EVIDENCE.md) proves issuance and rotation inside the host-agent namespace and the cross-container socket boundary. |
| 7 | Incus API authentication and least privilege | **PARTIAL; production blocker** | [`../p5/EVIDENCE.md`](../p5/EVIDENCE.md) proves a clean project-confined read-only identity. Incus 7.3 cannot grant one-key config mutation: the smallest writer grant can edit all instance config, forge UUID anchors, rename, and delete instances. P11 does not accept that residual authority. |
| 8 | Selector derivation and plugin boundary | **PASS with anchor qualification** | [`../p6/EVIDENCE.md`](../p6/EVIDENCE.md) freezes the schema and lifecycle semantics. UUID and generation are stable where expected, but are writable and UUID uniqueness is not enforced. |
| 9 | Broker API integration, pinning, isolation, fallback, upgrade | **PASS for H6; production hardening required** | [`../p7/EVIDENCE.md`](../p7/EVIDENCE.md) proves H6, the vendor `type_url`, selector derivation, failure isolation, checksum enforcement, and error taxonomy on v1.15.2. The experimental API requires an upgrade gate; production should use image-integrated plugin delivery rather than the shared writable volume. |
| 10 | Nested VM and authoritative instance binding | **FUNCTIONALLY PASS; security blocker remains** | [`../p8/EVIDENCE.md`](../p8/EVIDENCE.md) proves H7 and server-side binding to live Incus state. The bearer value remains portable, and the writer credential can mutate the anchor the attestor trusts. |
| 11 | One-time nonce lifecycle and delivery | **PASS for lifecycle; bearer risk not accepted** | P8 proves expiry, atomic single use, concurrent redemption, generation invalidation, deletion handling, and reaping. Deliberate transfer proves an unused value is a portable bearer credential. |
| 12 | Guest `x509pop`, guest agent, and Workload API | **PASS for H8; production hardening required** | [`../p9/EVIDENCE.md`](../p9/EVIDENCE.md) proves the complete TPM-to-guest chain, non-root workload issuance, rotation, clean bootstrap, and fail-closed burn semantics. Private-key delivery, authority co-location, and node re-attestation remain unresolved production boundaries. |
| 13 | Adversarial and lifecycle matrix | **ACCEPTED with findings and one allowed deferral** | [`../p10/p10-matrix.md`](../p10/p10-matrix.md) accepts cases 1–4, 6–7, and 9–10; case 5 meets acceptance with the required bearer finding; case 8 defers only real cluster-member migration. Export/import directly reproduced duplicate authoritative anchors. |
| 14 | Observability and evidence without secret leakage | **PASS** | Phase evidence distinguishes direct observations from inferences and records machine-readable failure outcomes. Phase secret scans passed with working controls. P11 selected no recovery-key or credential content; its scan found no private-key boundary, long base64 candidate, or nonce assignment. |
| 15 | Cleanup and architecture decision | **PASS** | [`teardown.txt`](./teardown.txt), [`tpm-diff.txt`](./tpm-diff.txt), and [`final-state.txt`](./final-state.txt) prove ordered deletion, an empty post-reboot workload and spike-resource inventory, unchanged TPM namespaces, trusted TPM unlock, and the final decision record. |
| 16 | Implementation artifacts and file targets | **PASS for durable spike output** | Durable code remains on `feat/incus-attestor`; Appendix C paths were implemented behind the measured package boundaries. Spike harnesses remain disposable and were not promoted as product code. |
| 17 | Tests for frozen observable contracts | **PASS** | [`../p7/code-verification.txt`](../p7/code-verification.txt) and later phase checkpoints record passing unit tests for selector derivation, lifecycle-shaped records, error taxonomy, retry bounds, plugin mappings, Broker client behavior, nonce lifecycle, broker behavior, build/vet, and `golangci-lint` with zero issues. Live P3–P10 phases provide end-to-end verification. |

## Coverage conclusion

All 17 required areas have evidence. Coverage completeness does not imply production acceptance. Rows 7, 10, 11, and 13 contain the direct contradictions that drive the P11 **NO-GO for adoption as-is**.
